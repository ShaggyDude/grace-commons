SPECIFICATION Spec
CONSTANTS
    ExpiresAt = 2
    MaxClock = 6
INVARIANT Safety
CHECK_DEADLOCK FALSE
\* Same constants as invitation.cfg (clamp saturates at ExpiresAt + 1 = 3). This
\* twin breaks single-resolution (AcceptBuggy drops the Pending guard); the harness
\* MUST reject it on Inv_SingleResolution.
