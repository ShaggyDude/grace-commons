SPECIFICATION Spec
CONSTANTS
    ExpiresAt = 2
    MaxClock = 6
INVARIANT Safety
CHECK_DEADLOCK FALSE
\* Buggy twin (FC F2): BrokenEffStatus echoes the stored state and never derives
\* "Expired", so Inv_LapsedReadsExpired is violated the moment a Pending record
\* lapses (now >= ExpiresAt). The harness MUST reject this twin. Same constants as
\* invitation.cfg so the saturation point matches (clamp at ExpiresAt + 1 = 3).
