SPECIFICATION Spec
CONSTANTS
    ExpiresAt = 2
    MaxClock = 6
INVARIANT Safety
CHECK_DEADLOCK FALSE
\* BOUND SATURATION (FC F1): Tick is clamped at ExpiresAt + 1, so exploration
\* saturates at the clock value ExpiresAt + 1 = 3 regardless of MaxClock. MaxClock
\* is set to 6 here (> ExpiresAt + 1) precisely to demonstrate that the extra
\* domain headroom adds NO reachable states: the model holds at 16 states whether
\* MaxClock is 3, 6, or larger. Reachable clock values: {0,1,2,3}. Saturated state
\* count: 16 (4 Pending states at now in {0,1,2,3}; 12 resolved states = 3 stored
\* terminals x 4 reachable clock values each). This is the real saturation point;
\* the pre-fix model grew without bound because Tick advanced to MaxClock.
