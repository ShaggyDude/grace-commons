This directory is intentionally empty. Formal models live beside their twin spec files:

- Alloy atom models: `atoms/<category>/<name>.als` beside `atoms/<category>/<name>.md`
- TLA+ composition models: `compositions/<name>.tla` beside `compositions/<name>.md`
