SPECIFICATION Spec

INVARIANT Safety

CHECK_DEADLOCK FALSE

CONSTANTS
    CredentialIds = {c1, c2}
    SessionTokens = {s1, s2, s3}
    MaxClock      = 3

\* To run from the command line:
\*   java -jar tla2tools.jar -config login.cfg \
\*        -workers 4 login.tla
