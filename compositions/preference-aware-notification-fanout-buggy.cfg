SPECIFICATION Spec
INVARIANT Safety
CHECK_DEADLOCK FALSE
CONSTANTS
    Cap = 2
    Workers = {w1, w2, w3}
\* Same constants as the correct model: Workers = Cap + 1 is the minimal
\* configuration in which the split observe/commit overshoots (one delivery
\* commits, then two workers both observe the last slot's headroom and both
\* commit -> delivered = 3 > Cap = 2). Expected: Inv4_CapSafety VIOLATED.
