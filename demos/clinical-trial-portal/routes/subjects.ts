// routes/subjects.ts
//
// Study Coordinator surface: enroll subjects and record visits
// TODO: Phase 4 — Implement:
//   - GET /subjects: list subjects
//   - GET /subjects/new: render enrollment form
//   - POST /subjects: enroll subject, emit subject.enrolled
//   - GET /subjects/:id: subject detail and visit history
//   - POST /subjects/:id/visits: record visit, emit visit.recorded
